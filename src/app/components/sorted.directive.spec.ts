import { SortedDirective } from './sorted.directive';

describe('SortedDirective', () => {
  it('should create an instance', () => {
    const directive = new SortedDirective();
    expect(directive).toBeTruthy();
  });
});
